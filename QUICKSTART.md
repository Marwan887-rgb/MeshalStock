# ⚡ دليل البدء السريع - MeshalStock

## 🚀 خطوات سريعة للتشغيل (5 دقائق)

### 1️⃣ تثبيت المتطلبات
```bash
pip install -r requirements.txt
```

### 2️⃣ إنشاء ملف البيئة
```bash
# انسخ الملف
copy .env.example .env

# أو في Linux/Mac
cp .env.example .env
```

### 3️⃣ (اختياري) تعديل الإعدادات
افتح `.env` وعدّل:
```env
ADMIN_PASSWORD=كلمة_مرورك_هنا
```

### 4️⃣ تشغيل التطبيق
```bash
python api_server.py
```

### 5️⃣ افتح المتصفح
```
http://localhost:5000
```

**كلمة المرور الافتراضية:** `admin123`

---

## 🎯 أول استخدام

### 1. تسجيل الدخول
- أدخل كلمة المرور: `admin123`
- اضغط "دخول"

### 2. تحديث البيانات (أول مرة)
- من القائمة اليمنى، اضغط "تحديث البيانات"
- اختر "الأسهم السعودية" أو "الأسهم الأمريكية"
- انتظر حتى ينتهي التحميل (قد يستغرق 10-30 دقيقة أول مرة)

### 3. استكشف الميزات
- **الرئيسية**: شاهد المؤشرات المباشرة
- **قائمة الأسهم**: تصفح جميع الأسهم
- **جان وفيبو**: حلل أي سهم
- **فلترة**: اكتشف الفرص تلقائياً

---

## ⚙️ إعدادات متقدمة

### تغيير المنفذ:
في `.env`:
```env
PORT=8080
```

### السماح بالوصول من الشبكة:
في `.env`:
```env
HOST=0.0.0.0
```
ثم افتح: `http://your-ip:5000`

### تعطيل Rate Limiting (للتطوير):
```env
RATE_LIMIT_ENABLED=False
```

---

## 🛑 إيقاف التطبيق

اضغط `Ctrl + C` في نافذة الـ Terminal

---

## ❓ حل المشاكل السريع

### "ModuleNotFoundError"
```bash
pip install -r requirements.txt
```

### "Port already in use"
غيّر PORT في `.env` أو أوقف البرنامج الآخر

### "No module named 'api_server'"
تأكد أنك في المجلد الصحيح:
```bash
cd MeshalStock
python api_server.py
```

### بيانات قديمة
استخدم "تحديث البيانات" من القائمة

---

## 📖 للمزيد

- **دليل شامل**: اقرأ [README.md](README.md)
- **دليل النشر**: اقرأ [DEPLOYMENT.md](DEPLOYMENT.md)
- **الأمان**: اقرأ [SECURITY.md](SECURITY.md)

---

**استمتع! 🎉**
